# Third-Party Licenses

This file is generated from `Cargo.lock` with `cargo-about`.
Do not edit it by hand; run `make third-party-licenses`.

## Runtime Dependencies

| Crate | Version | License | Source |
| --- | --- | --- | --- |
{{#each crates}}{{#if package.source}}| {{{package.name}}} | {{{package.version}}} | `{{{license}}}` | {{#if package.repository}}{{{package.repository}}}{{else}}https://crates.io/crates/{{{package.name}}}{{/if}} |
{{/if}}{{/each}}

## License Texts

{{#each licenses}}{{#if used_by.[0].crate.source}}
### {{{name}}} (`{{{id}}}`)

Used by:

{{#each used_by}}{{#if crate.source}}- {{{crate.name}}} {{{crate.version}}} ({{#if crate.repository}}{{{crate.repository}}}{{else}}https://crates.io/crates/{{{crate.name}}}{{/if}})
{{/if}}{{/each}}

```text
{{{text}}}
```

{{/if}}{{/each}}
